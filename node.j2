require "language/node"

class Node{{ class_name }} < Formula
  desc "{{ description }}"
  homepage "{{ links.homepage }}"
  version "{{ version }}"
  url "{{ url }}"
  sha256 "{{ sha256 }}"

  depends_on "node"

  def install
    system "npm", "install", *Language::Node.std_npm_install_args(libexec)
    bin.install_symlink Dir["#{libexec}/bin/*"]
  end

end
